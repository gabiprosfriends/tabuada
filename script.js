const numElement = document.getElementById('num');
const resultElement = document.getElementById('result');

    for(let i = 0; i <= 10; i++)
    {
        resultElement = numElement * i;
        (numElement, i , resultElement);
    }